export const environment = {
  production: true,
  // Todo: change localhost to the dockername
  // for local docker testing
  // API_URL: "http://localhost:8082/kniffel/api",
  // for server deployment
  API_URL: "kniffel/api"
};
